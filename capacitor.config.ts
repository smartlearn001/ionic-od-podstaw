import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'kurs-ionic-od-podstaw',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
